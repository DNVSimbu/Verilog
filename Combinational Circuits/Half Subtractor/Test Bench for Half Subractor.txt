// Test Bench for Half subractor

module halfsubractor_testbench;

reg a,b;
wire diff,borrow;
half_subractor dut (a,b,diff,borrow);
initial
begin 
 $dumpfile("dump.vcd");
 $dumpvars;

     a=0; b=0;
 #10 a=0; b=1;
 #10 a=1; b=0;
 #10 a=1; b=1;
 #10 $finish;
$monitor("Time = %0t Input a=%b b=%b output Difference=%b Borrow=%b", $time, a, b, diff, borrow);
end
endmodule